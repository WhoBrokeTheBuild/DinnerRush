#pragma once

#ifndef TYPES_H
#define TYPES_H

#include <SDL.h>
#include <string>
#include <vector>
#include <map>

using std::string;
using std::vector;
using std::map;

typedef unsigned char byte;
typedef SDL_Point Point;
typedef SDL_Rect Rect;

#endif // TYPES_H